language: dart
dart:
 - dev

before_install:
  - pub global activate webdev


jobs:
  include:
    - stage: build
      script: webdev build

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: true
  local-dir: build
  on:
    branch: master